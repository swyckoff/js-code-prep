{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch ts-node on ${file}",
      "runtimeArgs": ["-r", "ts-node/register", "--loader", "ts-node/esm"],
      "args": ["${file}"],
      "console": "integratedTerminal"
    }
  ]
}
